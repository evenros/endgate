defmodule EngineeringAnalyticsTerminalTest do
  use ExUnit.Case
  doctest EngineeringAnalyticsTerminal

  test "greets the world" do
    assert EngineeringAnalyticsTerminal.hello() == :world
  end
end
